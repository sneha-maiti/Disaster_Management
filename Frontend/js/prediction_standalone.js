const DATA = { "source_window": "2016-2025", "hazards": { "all_disasters": [{ "year": 2016, "events": 350 }, { "year": 2017, "events": 365 }, { "year": 2018, "events": 325 }, { "year": 2019, "events": 414 }, { "year": 2020, "events": 414 }, { "year": 2021, "events": 438 }, { "year": 2022, "events": 439 }, { "year": 2023, "events": 450 }, { "year": 2024, "events": 443 }, { "year": 2025, "events": 364 }], "all_disasters_excluding_earthquakes": [{ "year": 2016, "events": 320 }, { "year": 2017, "events": 343 }, { "year": 2018, "events": 305 }, { "year": 2019, "events": 383 }, { "year": 2020, "events": 398 }, { "year": 2021, "events": 410 }, { "year": 2022, "events": 408 }, { "year": 2023, "events": 418 }, { "year": 2024, "events": 429 }, { "year": 2025, "events": 335 }], "all_disasters_excluding_extreme_temperature": [{ "year": 2016, "events": 337 }, { "year": 2017, "events": 354 }, { "year": 2018, "events": 298 }, { "year": 2019, "events": 391 }, { "year": 2020, "events": 409 }, { "year": 2021, "events": 435 }, { "year": 2022, "events": 399 }, { "year": 2023, "events": 405 }, { "year": 2024, "events": 393 }, { "year": 2025, "events": 363 }], "drought": [{ "year": 2016, "events": 32 }, { "year": 2017, "events": 27 }, { "year": 2018, "events": 20 }, { "year": 2019, "events": 28 }, { "year": 2020, "events": 20 }, { "year": 2021, "events": 25 }, { "year": 2022, "events": 39 }, { "year": 2023, "events": 20 }, { "year": 2024, "events": 21 }, { "year": 2025, "events": 12 }], "earthquake": [{ "year": 2016, "events": 30 }, { "year": 2017, "events": 22 }, { "year": 2018, "events": 20 }, { "year": 2019, "events": 31 }, { "year": 2020, "events": 16 }, { "year": 2021, "events": 28 }, { "year": 2022, "events": 31 }, { "year": 2023, "events": 32 }, { "year": 2024, "events": 14 }, { "year": 2025, "events": 29 }], "extreme_temperature": [{ "year": 2016, "events": 13 }, { "year": 2017, "events": 11 }, { "year": 2018, "events": 27 }, { "year": 2019, "events": 23 }, { "year": 2020, "events": 5 }, { "year": 2021, "events": 3 }, { "year": 2022, "events": 40 }, { "year": 2023, "events": 45 }, { "year": 2024, "events": 50 }, { "year": 2025, "events": 1 }], "extreme_weather": [{ "year": 2016, "events": 87 }, { "year": 2017, "events": 130 }, { "year": 2018, "events": 99 }, { "year": 2019, "events": 95 }, { "year": 2020, "events": 133 }, { "year": 2021, "events": 118 }, { "year": 2022, "events": 109 }, { "year": 2023, "events": 140 }, { "year": 2024, "events": 149 }, { "year": 2025, "events": 156 }], "flood": [{ "year": 2016, "events": 164 }, { "year": 2017, "events": 132 }, { "year": 2018, "events": 127 }, { "year": 2019, "events": 193 }, { "year": 2020, "events": 207 }, { "year": 2021, "events": 224 }, { "year": 2022, "events": 182 }, { "year": 2023, "events": 168 }, { "year": 2024, "events": 148 }, { "year": 2025, "events": 120 }], "landslide": [{ "year": 2016, "events": 13 }, { "year": 2017, "events": 26 }, { "year": 2018, "events": 13 }, { "year": 2019, "events": 26 }, { "year": 2020, "events": 19 }, { "year": 2021, "events": 12 }, { "year": 2022, "events": 17 }, { "year": 2023, "events": 24 }, { "year": 2024, "events": 33 }, { "year": 2025, "events": 19 }], "volcanic_activity": [{ "year": 2016, "events": 1 }, { "year": 2017, "events": 2 }, { "year": 2018, "events": 9 }, { "year": 2019, "events": 4 }, { "year": 2020, "events": 4 }, { "year": 2021, "events": 9 }, { "year": 2022, "events": 5 }, { "year": 2023, "events": 4 }, { "year": 2024, "events": 7 }, { "year": 2025, "events": 4 }], "wildfire": [{ "year": 2016, "events": 10 }, { "year": 2017, "events": 15 }, { "year": 2018, "events": 10 }, { "year": 2019, "events": 14 }, { "year": 2020, "events": 10 }, { "year": 2021, "events": 19 }, { "year": 2022, "events": 16 }, { "year": 2023, "events": 17 }, { "year": 2024, "events": 21 }, { "year": 2025, "events": 23 }] }, "earthquake_monthly": [{ "month": "2016-01", "events": 1157 }, { "month": "2016-02", "events": 1088 }, { "month": "2016-03", "events": 1094 }, { "month": "2016-04", "events": 1436 }, { "month": "2016-05", "events": 1176 }, { "month": "2016-06", "events": 1277 }, { "month": "2016-07", "events": 1179 }, { "month": "2016-08", "events": 1404 }, { "month": "2016-09", "events": 1447 }, { "month": "2016-10", "events": 1061 }, { "month": "2016-11", "events": 1475 }, { "month": "2016-12", "events": 1617 }, { "month": "2017-01", "events": 1155 }, { "month": "2017-02", "events": 885 }, { "month": "2017-03", "events": 1000 }, { "month": "2017-04", "events": 1091 }, { "month": "2017-05", "events": 1080 }, { "month": "2017-06", "events": 941 }, { "month": "2017-07", "events": 1222 }, { "month": "2017-08", "events": 975 }, { "month": "2017-09", "events": 1368 }, { "month": "2017-10", "events": 1043 }, { "month": "2017-11", "events": 1342 }, { "month": "2017-12", "events": 1011 }, { "month": "2018-01", "events": 1210 }, { "month": "2018-02", "events": 1079 }, { "month": "2018-03", "events": 1099 }, { "month": "2018-04", "events": 1017 }, { "month": "2018-05", "events": 1079 }, { "month": "2018-06", "events": 1055 }, { "month": "2018-07", "events": 1100 }, { "month": "2018-08", "events": 1574 }, { "month": "2018-09", "events": 1384 }, { "month": "2018-10", "events": 1415 }, { "month": "2018-11", "events": 1162 }, { "month": "2018-12", "events": 1439 }, { "month": "2019-01", "events": 1204 }, { "month": "2019-02", "events": 1042 }, { "month": "2019-03", "events": 1129 }, { "month": "2019-04", "events": 1164 }, { "month": "2019-05", "events": 1019 }, { "month": "2019-06", "events": 1197 }, { "month": "2019-07", "events": 1322 }, { "month": "2019-08", "events": 958 }, { "month": "2019-09", "events": 993 }, { "month": "2019-10", "events": 1093 }, { "month": "2019-11", "events": 1180 }, { "month": "2019-12", "events": 1254 }, { "month": "2020-01", "events": 1378 }, { "month": "2020-02", "events": 1011 }, { "month": "2020-03", "events": 1082 }, { "month": "2020-04", "events": 1112 }, { "month": "2020-05", "events": 1232 }, { "month": "2020-06", "events": 1233 }, { "month": "2020-07", "events": 1253 }, { "month": "2020-08", "events": 1111 }, { "month": "2020-09", "events": 1147 }, { "month": "2020-10", "events": 1267 }, { "month": "2020-11", "events": 1022 }, { "month": "2020-12", "events": 1119 }, { "month": "2021-01", "events": 1170 }, { "month": "2021-02", "events": 1458 }, { "month": "2021-03", "events": 2113 }, { "month": "2021-04", "events": 1278 }, { "month": "2021-05", "events": 1339 }, { "month": "2021-06", "events": 1199 }, { "month": "2021-07", "events": 1300 }, { "month": "2021-08", "events": 2222 }, { "month": "2021-09", "events": 1325 }, { "month": "2021-10", "events": 1155 }, { "month": "2021-11", "events": 1242 }, { "month": "2021-12", "events": 1496 }, { "month": "2022-01", "events": 1458 }, { "month": "2022-02", "events": 1260 }, { "month": "2022-03", "events": 1569 }, { "month": "2022-04", "events": 1360 }, { "month": "2022-05", "events": 1166 }, { "month": "2022-06", "events": 1209 }, { "month": "2022-07", "events": 1334 }, { "month": "2022-08", "events": 1205 }, { "month": "2022-09", "events": 1533 }, { "month": "2022-10", "events": 1231 }, { "month": "2022-11", "events": 1273 }, { "month": "2022-12", "events": 1151 }, { "month": "2023-01", "events": 1385 }, { "month": "2023-02", "events": 1524 }, { "month": "2023-03", "events": 1239 }, { "month": "2023-04", "events": 1174 }, { "month": "2023-05", "events": 1444 }, { "month": "2023-06", "events": 1252 }, { "month": "2023-07", "events": 1116 }, { "month": "2023-08", "events": 1097 }, { "month": "2023-09", "events": 1112 }, { "month": "2023-10", "events": 1392 }, { "month": "2023-11", "events": 1418 }, { "month": "2023-12", "events": 2078 }, { "month": "2024-01", "events": 1389 }, { "month": "2024-02", "events": 1147 }, { "month": "2024-03", "events": 1122 }, { "month": "2024-04", "events": 1548 }, { "month": "2024-05", "events": 1208 }, { "month": "2024-06", "events": 1109 }, { "month": "2024-07", "events": 1096 }, { "month": "2024-08", "events": 1107 }, { "month": "2024-09", "events": 1041 }, { "month": "2024-10", "events": 1108 }, { "month": "2024-11", "events": 1052 }, { "month": "2024-12", "events": 1248 }, { "month": "2025-01", "events": 1413 }, { "month": "2025-02", "events": 1394 }, { "month": "2025-03", "events": 1178 }, { "month": "2025-04", "events": 1317 }, { "month": "2025-05", "events": 1228 }, { "month": "2025-06", "events": 1156 }, { "month": "2025-07", "events": 2685 }, { "month": "2025-08", "events": 2223 }, { "month": "2025-09", "events": 1600 }, { "month": "2025-10", "events": 1509 }, { "month": "2025-11", "events": 1321 }, { "month": "2025-12", "events": 1277 }], "earthquake_event_count": 152412 };

(() => {
    'use strict';
    const LABELS = { flood: 'Flood', earthquake: 'Earthquake', wildfire: 'Wildfire', drought: 'Drought', landslide: 'Landslide', volcanic_activity: 'Volcanic activity', extreme_weather: 'Extreme weather', extreme_temperature: 'Extreme temperature' };
    const HAZARDS = Object.keys(LABELS);
    const $ = selector => document.querySelector(selector);
    const $$ = selector => Array.from(document.querySelectorAll(selector));
    const fmt = value => Number(value || 0).toLocaleString();
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    let selectedHazard = 'flood';
    let transitionToken = 0;

    const rowsFor = key => DATA.hazards[key] || [];
    const avg = rows => rows.length ? rows.reduce((sum, row) => sum + Number(row.events || 0), 0) / rows.length : 0;
    const scoreFor = rows => {
        if (!rows.length) return 0;
        const values = rows.map(row => Number(row.events || 0)), latest = values.at(-1), min = Math.min(...values), max = Math.max(...values), baseline = avg(rows);
        const recency = max === min ? .5 : (latest - min) / (max - min);
        const early = avg(rows.slice(0, 3)), recent = avg(rows.slice(-3));
        const trend = Math.max(-1, Math.min(1, (recent - early) / Math.max(baseline, 1)));
        return Math.max(0, Math.min(100, Math.round((recency * .65 + (trend * .5 + .5) * .35) * 100)));
    };
    const levelFor = score => score >= 75 ? 'CRITICAL' : score >= 50 ? 'HIGH' : score >= 25 ? 'MODERATE' : 'LOW';
    const colorFor = level => level === 'CRITICAL' || level === 'HIGH' ? 'var(--red)' : level === 'MODERATE' ? 'var(--amber)' : 'var(--green)';
    const sleep = ms => new Promise(resolve => setTimeout(resolve, ms));

    function countUp(element, target, suffix = '', duration = 700) {
        if (!element) return;
        if (reducedMotion) { element.textContent = target + suffix; return; }
        const start = performance.now(), from = Number(element.dataset.value || 0);
        const tick = now => { const progress = Math.min(1, (now - start) / duration), eased = 1 - Math.pow(1 - progress, 3), value = from + (target - from) * eased; element.textContent = (Number.isInteger(target) ? Math.round(value) : value.toFixed(1)) + suffix; if (progress < 1) requestAnimationFrame(tick); else element.dataset.value = target; };
        requestAnimationFrame(tick);
    }
    function staggerText(element, text) {
        if (!element) return;
        if (reducedMotion) { element.textContent = text; return; }
        element.classList.add('is-swapping');
        window.setTimeout(() => { element.textContent = text; element.classList.remove('is-swapping'); }, 160);
    }
    function toast(message) {
        let node = $('#aether-toast');
        if (!node) { node = document.createElement('div'); node.id = 'aether-toast'; node.style.cssText = 'position:fixed;right:24px;bottom:24px;z-index:100;color:#e9fbfc;background:rgba(7,25,31,.92);border:1px solid rgba(91,227,236,.5);box-shadow:0 16px 45px #0008,0 0 25px #5be3ec22;padding:14px 16px;font:800 10px "Courier New",monospace;letter-spacing:.08em;backdrop-filter:blur(16px);transform:translateY(16px);opacity:0;transition:.3s;pointer-events:none'; document.body.appendChild(node); }
        node.textContent = message; requestAnimationFrame(() => { node.style.opacity = '1'; node.style.transform = 'translateY(0)'; }); clearTimeout(node._timer); node._timer = setTimeout(() => { node.style.opacity = '0'; node.style.transform = 'translateY(16px)'; }, 2400);
    }
    function addRipple(event) {
        const target = event.currentTarget, rect = target.getBoundingClientRect(), ripple = document.createElement('span'); ripple.className = 'aether-ripple'; ripple.style.cssText = `position:absolute;left:${event.clientX - rect.left}px;top:${event.clientY - rect.top}px;width:8px;height:8px;border-radius:50%;background:#fff;opacity:.35;transform:translate(-50%,-50%) scale(0);pointer-events:none;animation:aetherRipple .65s ease-out forwards`; target.style.position = 'relative'; target.style.overflow = 'hidden'; target.appendChild(ripple); setTimeout(() => ripple.remove(), 700);
    }
    function installMicroInteractions() {
        const style = document.createElement('style'); style.textContent = '@keyframes aetherRipple{to{transform:translate(-50%,-50%) scale(42);opacity:0}}.is-swapping{opacity:.25;transform:translateY(3px);transition:.16s}.chart-tooltip{position:absolute;z-index:5;pointer-events:none;min-width:120px;padding:9px 10px;background:#07151b;border:1px solid rgba(91,227,236,.55);box-shadow:0 12px 28px #0008;color:#e9fbfc;font:800 10px "Courier New",monospace;letter-spacing:.05em;opacity:0;transform:translateY(5px);transition:.14s}.chart-tooltip.visible{opacity:1;transform:translateY(0)}'; document.head.appendChild(style);
        $$('.hazard-btn,.primary-action,.ghost-button,.back-button').forEach(button => button.addEventListener('click', addRipple));
        document.addEventListener('mousemove', event => { document.documentElement.style.setProperty('--mouse-x', `${event.clientX}px`); document.documentElement.style.setProperty('--mouse-y', `${event.clientY}px`); });
    }
    function installParticleField() {
        if (reducedMotion) return;
        const canvas = document.createElement('canvas'); canvas.setAttribute('aria-hidden', 'true'); canvas.style.cssText = 'position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:-1;opacity:.44'; document.body.appendChild(canvas); const ctx = canvas.getContext('2d'), particles = [];
        const resize = () => { const ratio = Math.min(window.devicePixelRatio || 1, 2); canvas.width = innerWidth * ratio; canvas.height = innerHeight * ratio; ctx.setTransform(ratio, 0, 0, ratio, 0, 0); }; resize(); window.addEventListener('resize', resize, { passive: true });
        const total = Math.min(75, Math.floor(innerWidth / 18)); for (let i = 0; i < total; i++)particles.push({ x: Math.random() * innerWidth, y: Math.random() * innerHeight, r: Math.random() * 1.5 + .35, vx: (Math.random() - .5) * .12, vy: -Math.random() * .2 - .04, a: Math.random() * .65 + .15 });
        const render = () => { ctx.clearRect(0, 0, innerWidth, innerHeight); for (const p of particles) { p.y += p.vy; p.x += p.vx; if (p.y < -5) p.y = innerHeight + 5; if (p.x < -5) p.x = innerWidth + 5; if (p.x > innerWidth + 5) p.x = -5; ctx.beginPath(); ctx.fillStyle = `rgba(91,227,236,${p.a})`; ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2); ctx.fill(); } requestAnimationFrame(render); }; render();
    }
    function sparkline(rows) {
        if (!rows.length) return '';
        const values = rows.map(row => row.events), min = Math.min(...values), max = Math.max(...values), w = 130, h = 35, points = rows.map((row, index) => `${index / (rows.length - 1 || 1) * w},${h - 4 - ((row.events - min) / Math.max(max - min, 1)) * (h - 10)}`).join(' ');
        return `<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><polyline points="${points}" fill="none" stroke="var(--cyan)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>`;
    }
    function drawChart(element, rows, color = 'var(--cyan)', monthly = false) {
        if (!element) return;
        if (!rows.length) { element.innerHTML = '<p class="disclaimer">No observations embedded for this series.</p>'; return; }
        const width = 900, height = 280, pad = { l: 38, r: 16, t: 18, b: 36 }, values = rows.map(row => Number(row.events || 0)), max = Math.max(...values) * 1.14 || 1;
        const x = index => pad.l + index * (width - pad.l - pad.r) / Math.max(1, rows.length - 1), y = value => height - pad.b - (value / max) * (height - pad.t - pad.b), points = rows.map((row, index) => [x(index), y(row.events)]);
        const line = points.map((point, index) => (index ? 'L' : 'M') + point[0].toFixed(2) + ' ' + point[1].toFixed(2)).join(' '), area = line + ` L ${points.at(-1)[0]} ${height - pad.b} L ${points[0][0]} ${height - pad.b} Z`;
        let grid = ''; for (let i = 0; i < 4; i++) { const gy = pad.t + i * (height - pad.t - pad.b) / 3; grid += `<line class="grid-line" x1="${pad.l}" y1="${gy}" x2="${width - pad.r}" y2="${gy}"/><text class="axis-label" x="5" y="${gy + 4}">${Math.round(max - (max / 3) * i).toLocaleString()}</text>`; }
        const step = Math.max(1, Math.ceil(rows.length / 7)); let labels = ''; rows.forEach((row, index) => { if (index % step === 0) { const text = monthly ? row.month : row.year; labels += `<text class="axis-label" x="${x(index) - 14}" y="${height - 10}">${text}</text>`; } });
        const dots = points.map((point, index) => `<circle class="trend-point" cx="${point[0]}" cy="${point[1]}" r="3" data-index="${index}"><title>${monthly ? rows[index].month : rows[index].year}: ${fmt(rows[index].events)}</title></circle>`).join('');
        element.innerHTML = `<svg viewBox="0 0 ${width} ${height}" preserveAspectRatio="none"><defs><linearGradient id="chart-area-gradient" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stop-color="#5be3ec" stop-opacity=".28"/><stop offset="1" stop-color="#5be3ec" stop-opacity="0"/></linearGradient></defs>${grid}<path class="area-fill" d="${area}"/><path class="trend-line" d="${line}" style="stroke:${color}"/>${dots}${labels}</svg>`;
        const svg = element.querySelector('svg'); let tooltip = element.querySelector('.chart-tooltip'); if (!tooltip) { tooltip = document.createElement('div'); tooltip.className = 'chart-tooltip'; element.style.position = 'relative'; element.appendChild(tooltip); }
        element.querySelectorAll('.trend-point').forEach(point => point.addEventListener('mouseenter', event => { const row = rows[Number(event.currentTarget.dataset.index)]; tooltip.innerHTML = `<strong>${monthly ? row.month : row.year}</strong><br>${fmt(row.events)} reported events`; tooltip.style.left = Math.min(element.clientWidth - 135, Math.max(8, event.offsetX - 55)) + 'px'; tooltip.style.top = Math.max(8, event.offsetY - 52) + 'px'; tooltip.classList.add('visible'); }));
        element.addEventListener('mouseleave', () => tooltip.classList.remove('visible'));
    }
    function renderButtons() {
        const holder = $('#hazard-selector'); if (!holder) return; holder.innerHTML = HAZARDS.map((key, index) => `<button class="hazard-btn ${key === selectedHazard ? 'active' : ''}" data-hazard="${key}" aria-pressed="${key === selectedHazard}" title="Shortcut ${index + 1}">${LABELS[key]}</button>`).join('');
        $$('[data-hazard]').forEach(button => button.addEventListener('click', () => selectHazard(button.dataset.hazard)));
    }
    function renderTable(rows, baseline) {
        const table = $('#data-table'); if (!table) return; table.innerHTML = rows.map(row => { const index = row.events / Math.max(baseline, 1) * 100, signal = index >= 120 ? 'ELEVATED' : index <= 80 ? 'QUIET' : 'BASELINE', color = signal === 'ELEVATED' ? 'var(--red)' : signal === 'QUIET' ? 'var(--green)' : 'var(--amber)'; return `<tr><td>${row.year}</td><td>${fmt(row.events)}</td><td>${index.toFixed(0)}%</td><td><span class="signal-chip" style="color:${color}">${signal}</span></td></tr>`; }).join('');
    }
    function updateRiskMeter(score, level) {
        const meter = $('#risk-meter'), color = colorFor(level); if (!meter) return; meter.style.setProperty('--score', score + '%'); meter.style.setProperty('--risk-color', color); const scoreElement = $('#risk-score'); scoreElement.dataset.value = 0; countUp(scoreElement, score, '%'); const badge = $('#risk-badge'); if (badge) { badge.textContent = level; badge.style.color = color; badge.style.borderColor = color; } const strength = Math.max(12, Math.min(100, score + 18)); const bar = $('#signal-strength'); if (bar) { bar.style.width = '0%'; requestAnimationFrame(() => bar.style.width = strength + '%'); } if ($('#signal-value')) countUp($('#signal-value'), strength, '%'); if ($('#hero-score')) countUp($('#hero-score'), score, '%');
    }
    async function selectHazard(key) {
        if (!LABELS[key]) return; selectedHazard = key; const token = ++transitionToken, rows = rowsFor(key), values = rows.map(row => row.events), baseline = avg(rows), latest = values.at(-1) || 0, first = values[0] || 0, score = scoreFor(rows), level = levelFor(score), delta = (latest - first) / Math.max(first, 1) * 100;
        renderButtons(); document.body.classList.add('is-loading-hazard'); if (!reducedMotion) await sleep(90); if (token !== transitionToken) return;
        updateRiskMeter(score, level); staggerText($('#hazard-title'), LABELS[key]); if ($('#risk-subtitle')) $('#risk-subtitle').textContent = `${level.toLowerCase()} historical-pattern signal / ${DATA.source_window}`; if ($('#risk-explanation')) $('#risk-explanation').textContent = `The ${LABELS[key].toLowerCase()} signal is ${score}%. This indicator combines latest activity, position within the ten-year range, and recent-versus-early trend. It is not a calibrated probability.`; countUp($('#latest-events'), latest); countUp($('#average-events'), baseline); if ($('#change-events')) $('#change-events').textContent = (delta >= 0 ? '+' : '') + delta.toFixed(1) + '%'; if ($('#mini-spark')) $('#mini-spark').innerHTML = sparkline(rows); if ($('#table-title')) $('#table-title').textContent = LABELS[key] + ' / annual observations'; if ($('#trend-caption')) $('#trend-caption').textContent = DATA.source_window; if ($('#peak-caption')) { const peak = Math.max(...values); $('#peak-caption').textContent = 'PEAK: ' + fmt(peak) + ' / ' + rows[values.indexOf(peak)].year; } if ($('#early-period')) countUp($('#early-period'), first); if ($('#late-period')) countUp($('#late-period'), latest); if ($('#comparison-text')) $('#comparison-text').textContent = `The latest observation is ${Math.abs(delta).toFixed(1)}% ${delta >= 0 ? 'above' : 'below'} the first year in the embedded window.`; if ($('#insight-title')) $('#insight-title').textContent = level === 'LOW' ? 'Quiet relative to the observed range' : level === 'MODERATE' ? 'Mixed signal / monitor the trend' : 'Elevated activity relative to baseline'; if ($('#insight-text')) $('#insight-text').textContent = level === 'LOW' ? 'The latest observation is near the quieter end of the historical range. Continue monitoring because low reported activity does not eliminate local risk.' : level === 'MODERATE' ? 'The latest observation sits in a mixed historical zone. Additional local data and official advisories are needed before making decisions.' : 'The latest observation is elevated within this embedded series. Treat this as a prioritization signal, not a warning, and consult official sources.'; renderTable(rows, baseline); drawChart($('#annual-chart'), rows); const seismic = $('#seismic-section'); if (seismic) seismic.classList.toggle('hidden', key !== 'earthquake'); document.body.classList.remove('is-loading-hazard');
    }
    function exportView() {
        const rows = rowsFor(selectedHazard), baseline = avg(rows), csv = ['year,events,index_vs_baseline,signal', ...rows.map(row => { const index = row.events / Math.max(baseline, 1) * 100; return `${row.year},${row.events},${index.toFixed(1)},${index >= 120 ? 'ELEVATED' : index <= 80 ? 'QUIET' : 'BASELINE'}`; })].join('\n'), blob = new Blob([csv], { type: 'text/csv;charset=utf-8' }), url = URL.createObjectURL(blob), anchor = document.createElement('a'); anchor.href = url; anchor.download = `aether-${selectedHazard}-historical-view.csv`; anchor.click(); URL.revokeObjectURL(url); toast(`${LABELS[selectedHazard].toUpperCase()} VIEW EXPORTED`);
    }
    function installKeyboardShortcuts() {
        document.addEventListener('keydown', event => { if (event.target.matches('input,textarea,select')) return; const number = Number(event.key); if (number >= 1 && number <= HAZARDS.length) selectHazard(HAZARDS[number - 1]); if (event.key.toLowerCase() === 'e') exportView(); });
    }
    function updateClock() { const now = new Date(); const clock = $('#clock'); if (clock) clock.textContent = now.toISOString().slice(11, 19) + ' UTC'; }
    function init() {
        if (!$('#hazard-selector')) return;
        installMicroInteractions(); installParticleField(); installKeyboardShortcuts();
        if ($('#embedded-count')) $('#embedded-count').textContent = fmt(DATA.earthquake_event_count); if ($('#seismic-count')) $('#seismic-count').textContent = fmt(DATA.earthquake_event_count); if ($('#data-window')) $('#data-window').textContent = DATA.source_window.replace('-', '–');
        renderButtons(); drawChart($('#earthquake-chart'), DATA.earthquake_monthly, 'var(--cyan)', true); selectHazard('flood');
        $('#scroll-to-analysis')?.addEventListener('click', () => $('#analysis')?.scrollIntoView({ behavior: reducedMotion ? 'auto' : 'smooth' })); $('#download-data')?.addEventListener('click', exportView); updateClock(); setInterval(updateClock, 1000); toast('HISTORICAL MODEL READY');
    }
    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init, { once: true }); else init();
})();
